# 风格提示词 · 暖阳信笺风 ☀✉

> 本项目的视觉风格规范。以后新增页面、组件或让 AI 生成同风格内容时，
> 直接把本文（尤其是末尾的「AI 复用提示词」）喂给它即可保持一致。

## 一句话定位

像午后阳光晒着的旧书桌：牛皮纸信封、火漆封印、复古邮戳，一切温暖、
缓慢、有手作质感，让读信成为一场小小的仪式。

**氛围关键词**：复古浪漫 · 温暖治愈 · 手作质感 · 怀旧温馨 · 柔和细腻 · 留白呼吸

## 色板（精确值，改一处需全文对照）

| 用途 | 值 |
| --- | --- |
| 背景渐变（上→下） | `#f9eed8 → #f4e4c2 → #f0dcb6 → #dcbf8f`（米黄→奶油→浅驼） |
| 牛皮纸信封 | 主 `#c9a874` / 深 `#bd935e` / 边 `#a67b46`，口袋高光从 `#d2ac7c` 起 |
| 信纸 | `#fdf6e9`（近白米色） |
| 火漆 | 主 `#a8352a` / 暗 `#7e241c` / 高光 `#cf5c4c`（径向渐变+浮雕内阴影） |
| 点缀金 | `#d9a441`（装饰星、涟漪、聚焦框），深金 `#a9843f` |
| 墨色文字 | 主 `#5d4632`（深暖棕），次级 `#6d5540` |
| 邮戳墨 | `#6b5a8a`（紫墨，配 `mix-blend-mode: multiply` + 68% 不透明度） |
| 木质托盘 | `#ab7c4d → #8a5f38 → #754d2b` + 横向细木纹 |
| 托盘内文字 | `#fcf3de` / `rgba(253,246,233,.78)`（深底必须用米白） |
| 恋爱计时 | `#7d4034`（红棕）on `rgba(255,248,232,.62)` |

## 字体

- 全站楷体栈：`"Kaiti SC","STKaiti","KaiTi","FZKai-Z03S","AR PL UKai CN",serif`
  （手写信气质、零字体下载）
- 标题：`clamp(30px,5vw,44px)`，字距 `.14em`，两侧 ✦ 金色小星装饰
- 副标题/小字：宽字距是本风格的灵魂——副标题 `.22em`、日期 `.3em`、提示语 `.28em`
- 邮戳/邮票内文字：小号、居中，允许 `serif`

## 形状与质感

- **圆角**：卡片 10–16px、信封 3–6px，模拟手作物的柔和边缘
- **有机形状**：火漆用非正圆 `border-radius: 47% 53% 51% 49% / 52% 48% 56% 44%`
- **三角形**：信封上盖/口袋用 `clip-path: polygon()` 剪出
- **齿孔**：邮票边缘用 SVG mask 沿四边打圆孔
- **纸纹噪点**：全屏 `feTurbulence` 噪点层，`opacity .07`
- **暗角**：上亮下暗双径向渐变，把视线收向中心
- **和纸胶带**：读信页页角贴米色半透明胶带（`rgba(233,196,150,.82)`，微旋转）

## 动效语言（慢而柔，带一点弹性）

| 动效 | 时长 / 缓动 |
| --- | --- |
| 信封待机浮动 | 3.6s 循环，translateY ±3~4px |
| 入场 riseIn | .9s，标题→信封→托盘 按 .1/.3/.5s 错峰 |
| 提示呼吸 | 2.8s，透明度 .72↔1（不许低于 .7） |
| 火漆消散 | .42s `cubic-bezier(.3,.7,.4,1)`，scale 1→1.22 + 旋转 −9° + 淡出 |
| 金色涟漪 | .55s ease-out，scale .7→2.7 |
| 上盖翻开 | .62s `cubic-bezier(.35,.1,.25,1)`，rotateX 168°（origin 顶边） |
| 信纸滑出 | .7s `cubic-bezier(.3,1.25,.4,1)`（带回弹过冲），translateY −52% |
| 读信浮层 | .35s 淡入 + 面板 `translateY(16px)→0` |
| 火漆重凝 | .55s `cubic-bezier(.3,1.2,.4,1)`，scale .35→1.12→1 |
| 粒子 | 背景暖光斑 16–46 颗缓浮；拆封迸溅 34 颗金屑向上扬、0.6–1.1s 落下 |

原则：装饰性动画永远可被 `prefers-reduced-motion` 关掉；时长宁慢勿快
（拆信全流程约 2.2s，仪式感优先于效率）。

## 氛围层

暖色光斑 canvas（背景层 z0）+ 迸溅专用前景 canvas（z90，浮于内容、
低于浮层）+ 噪点 + 暗角。光斑色：`#ffe096 / #fff3d6 / #ffc98b`。

## 文案语气

书信体、温柔克制：「见字如面」「纸短情长」「一封一封，慢慢写给你」。
装饰符号用 ✦ ❋ ✧ ♡ ▾，不用粗体感叹和大写强调。

## 对比度红线（踩过的坑）

- 深色木底上必须用米白文字（`#fcf3de` 系），禁止深棕
- 浅底上次级文字不低于 `#6d5540`（小字号对比度 ≥ 4.5:1）
- 呼吸/闪烁动画的透明度下限 0.7

---

## AI 复用提示词（复制即用）

> 为一个「手写信箱」主题网页生成 UI。风格：复古浪漫、温暖治愈的手作信笺风，
> 像午后旧书桌上的一封牛皮纸信。配色：米黄渐变背景（#f9eed8→#dcbf8f）、
> 牛皮纸信封（#c9a874/#bd935e）、深红火漆（#a8352a，径向渐变+浮雕阴影+有机非正圆轮廓）、
> 点缀金（#d9a441）、暖棕墨色文字（#5d4632 主、#6d5540 次）、紫墨圆邮戳（#6b5a8a，
> multiply 混合）、深木托盘（#8a5f38）上放米白文字（#fcf3de）。字体用系统楷体
> （Kaiti 栈），标题带 .14em 字距和 ✦ 星饰，小字宽字距 .2–.3em。形状语言：大圆角、
> clip-path 三角信封盖、SVG 齿孔邮票、和纸胶带页角、feTurbulence 噪点 .07、上下暗角。
> 动效缓慢柔和带回弹：信封 3.6s 浮动、火漆 .42s 消散+金色涟漪、上盖 .62s rotateX 翻开、
> 信纸 .7s 滑出、浮层 .35s 淡入；canvas 暖色光斑缓浮，拆封时 34 颗金屑上扬。
> 文案是温柔书信体（见字如面/纸短情长），装饰用 ✦❋✧♡。遵守对比度红线：
> 深底米白字、浅底深棕字、闪烁不低于 70% 不透明度，且全部动画尊重
> prefers-reduced-motion。

---

# 特别主题 · 七夕星河（qixi skin）

## 概念

「换肤」式特别主题：布局与交互完全不变，整站从暖阳信笺切换成
七夕星夜——牛郎织女隔银河相望，鹊桥星弧横过天顶，喜鹊结队飞过，
金色火漆封着双子星封印。读这封信时天亮转夜（1.4s 交叉淡入），
合上信又回到午后书桌。

**氛围关键词**：午夜蓝紫 · 银河微光 · 金星点点 · 隔河相望 · 静谧深情

## 色板

| 用途 | 值 |
| --- | --- |
| 夜空渐变（上→下） | `#171034 → #241549 → #32205e → #1c1238` |
| 银河斜带 | 102° 斜向条纹，`rgba(168,140,255,.17)` 夹 `rgba(255,246,225,.11)` 亮芯 |
| 星云 | 紫 `rgba(122,82,204,.24)`（左下）、`rgba(92,62,186,.22)`（右上）、暖金 `rgba(255,205,120,.10)` |
| 星空信封 | 口袋 `#523a87→#372660`，上盖 `#372660→#2a1c4d`，底 `#43306e→#31215a` |
| 金色火漆 | `#f6dd96 → #d9a441 → #8f6a1c` 径向渐变，浮雕金阴影，封印图标=双子星 |
| 主文字 | `#f2ecff`，次级 `#c9bce8`（夜底浅紫白） |
| 邮戳墨 | `#baa7f5`（星紫） |
| 紫檀托盘 | `#5a3c86 → #341f4e` + 金色木纹线 |
| 装饰金 | 亮 `#f3d98b` / 深 `#d9c06a` / 文字辉光 `rgba(231,196,106,.6)` |

## 场景元素

- **织女星（左下）**：深蓝剪影 `#0e0a22` + 金描边，头顶四角亮星，
  手执金线；7s 缓浮。标签「织女星」金字带辉光
- **牛郎星（右下）**：剪影挑金色扁担，两端各挂一颗小星（孩子）；8s 反相缓浮
- **鹊桥**：1440 宽 SVG 星点弧线跨过天顶，两端大亮星，透明度 .55
- **喜鹊**：深蓝小鸟金描边、翅膀 .38s 振翅，3+2 两队错峰 17s/23s 横穿银河
- **星子粒子**：canvas 光斑变白色/暖金/淡紫小星（r 2–7），闪烁频率加倍、密度上调
- **拆封迸溅**：金屑 + 星紫碎屑（1/6 概率紫）
- **读信浮层**：面板保持奶油信纸不换色（真迹优先），遮罩加深为 `rgba(14,8,34,.66)`，
  和纸胶带变淡紫 `rgba(216,190,240,.75)`

## 七夕限定信封细节

- 邮票：藏蓝底 `#3b2a63` + 喜鹊踏枝金框图案 + 角标「七夕」
- 邮戳：顶带文字「七夕」、小鹊桥弧 + 双四角星注销线
- 封印：金色火漆 + 双子星（一大一小四角星）图案

## 动效与切换

- 主题切换：双层背景 div 交叉淡入 1.4s，装饰层 1.6s 延迟浮现（夜幕先落、星星后亮）
- 布局零改动；移动端 <860px 剪影缩小降透明度，<560px 隐藏剪影与鹊桥、只留银河星子

## AI 复用提示词 · 七夕星河（复制即用）

> 在「暖阳信笺手写信箱」基础上生成七夕特别主题皮肤：整站布局不变，
> 背景换成午夜蓝紫夜空（#171034→#32205e），叠加 102° 斜向银河亮带
> （rgba(168,140,255,.17)）与紫/金星云；文字换浅紫白（#f2ecff/#c9bce8）。
> 信封变星空蓝紫（#523a87→#2a1c4d），火漆变金色（#f6dd96→#8f6a1c）封双子星印。
> 左下织女、右下牛郎深蓝剪影（#0e0a22 金描边）各带头顶亮星、7–8s 缓浮；
> 天顶星点弧线鹊桥（金点虚线弧+两端亮星）；深蓝喜鹊金描边振翅结队横穿。
> canvas 粒子变白/金/淡紫星子、闪烁加快；拆封迸溅金屑掺星紫。
> 邮戳带「七夕」与小鹊桥弧，邮票为喜鹊踏枝藏蓝底。读信面板保持奶油纸色不换。
> 主题切换用 1.4s 双层背景交叉淡入，夜幕先落、星星后亮；尊重 prefers-reduced-motion。
